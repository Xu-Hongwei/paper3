# 论文3：当前问题认知、方法调整与后续任务路线

> 本文档用于总结当前阶段对遥感图文检索（RSITR）问题的重新认识、已有诊断证据、原始方法设计需要进行的调整，以及后续实验与模型开发的推荐顺序。

---

# 1. 当前阶段的核心认识

经过以下工作：

- CLIP Zero-shot / Full Fine-Tuning baseline；
- Validation 训练过程分析；
- Test T2I 排名错误案例分析；
- 全量 T2I 错误结构统计；
- Same-category-only retrieval；
- Entity / Attribute / Relation consistency diagnostic；

当前对问题的认识已经从最开始较宽泛的：

> “遥感图文之间存在 semantic gap，需要更加细粒度的跨模态对齐。”

进一步收缩为更加具体的问题：

\[
\boxed{
\text{遥感图文检索的主要剩余瓶颈，不是粗粒度场景识别，}
\quad
\text{而是在高度相似的同类场景内部完成精确实例辨识。}
}
\]

换句话说，当前 CLIP 已经能够较好地回答：

> “这是一张什么场景的图？”

但仍然不擅长回答：

> “在很多同类、视觉高度相似的候选图像中，到底哪一张才是这条 caption 对应的具体 GT 图像？”

因此，后续论文的核心出发点应该从宽泛的：

\[
\text{Cross-modal Semantic Gap}
\]

转向：

\[
\boxed{
\text{Structured Intra-category Fine-grained Discrimination}
}
\]

即：

> **结构化的类内细粒度跨模态辨识。**

---

# 2. 当前 Baseline

## 2.1 Vanilla CLIP Full Fine-Tuning

当前已经完成并锁定的正式 baseline：

- Backbone：OpenAI CLIP ViT-B/32 QuickGELU
- Dataset：RSICD
- Training pairs：39,310
- Unique train images：7,862
- Captions per image：5
- Batch size：64
- Optimizer：AdamW
- Learning rate：\(1\times10^{-5}\)
- Weight decay：0.01
- Epochs：30
- Scheduler：5% warmup + linear decay
- Checkpoint selection：Validation mR
- Loss：Diagonal symmetric CLIP InfoNCE

正式 Test 结果：

| Direction | R@1 | R@5 | R@10 | Mean |
|---|---:|---:|---:|---:|
| Image → Text | 17.02 | 35.22 | 48.86 | 33.70 |
| Text → Image | 11.36 | 34.78 | 50.58 | 32.24 |

\[
\boxed{\text{Test mR}=32.97}
\]

这个结果应永久保留为：

\[
\boxed{\text{CLIP-Diagonal Vanilla Baseline}}
\]

后续不覆盖、不删除。

---

# 3. Validation 训练过程：没有明显后期崩坏

## 3.1 I2T Validation

| Epoch | R@1 | R@5 | R@10 | Mean |
|---|---:|---:|---:|---:|
| 1 | 10.99 | 19.68 | 29.40 | 20.02 |
| 10 | 14.14 | 25.28 | 35.25 | 24.89 |
| 20 | 13.63 | 24.87 | 35.81 | 24.77 |
| 30 | 15.16 | 26.96 | 38.20 | 26.77 |

提升：

- R@1：+4.17
- R@5：+7.27
- R@10：+8.80
- Mean：+6.75

---

## 3.2 T2I Validation

| Epoch | R@1 | R@5 | R@10 | Mean |
|---|---:|---:|---:|---:|
| 1 | 9.15 | 28.92 | 44.01 | 27.36 |
| 10 | 13.25 | 37.87 | 53.43 | 34.85 |
| 20 | 13.66 | 38.90 | 53.57 | 35.38 |
| 30 | 13.52 | 40.89 | 55.86 | 36.75 |

提升：

- R@1：+4.37
- R@5：+11.96
- R@10：+11.85
- Mean：+9.40

因此：

\[
\boxed{
\text{Validation 上 I2T 和 T2I 都在持续改善，并不存在明显训练后期 collapse。}
}
\]

Test 上 T2I R@1 较低，不能简单解释为：

> “T2I 在训练后期没有学起来。”

真正的问题更可能是：

- Test split 的场景分布；
- 同类视觉近邻密度；
- caption 与具体图像绑定弱；
- 实例级细粒度区分不足。

---

# 4. Test T2I 全量错误结构

Test 一共：

\[
5465
\]

条 caption 查询。

按 GT rank 分组：

| GT rank group | 数量 | 占比 | Top1 同类别率 | Top1 exact-same-caption 率 | GT-Top1 图像余弦中位数 |
|---|---:|---:|---:|---:|---:|
| rank = 1 | 621 | 11.36% | 100.00% | 100.00% | 1.0000 |
| rank = 2~5 | 1280 | 23.42% | 76.80% | 1.72% | 0.7342 |
| rank = 6~10 | 863 | 15.79% | 77.75% | 2.55% | 0.7161 |
| rank >10 | 2701 | 49.42% | 67.64% | 1.96% | 0.6469 |

---

## 4.1 Exact duplicate caption 不是主要直接原因

错误组中 Top1 exact-same-caption 只有约：

\[
1.7\%\sim2.6\%
\]

因此：

\[
\boxed{
\text{Exact string duplication 不是当前 Test T2I 错误的主要原因。}
}
\]

但这并不意味着不存在：

\[
\text{Semantic Redundancy}
\]

例如：

```text
many buildings are beside a road
```

与：

```text
a road runs next to a dense group of buildings
```

字符串不同，但语义高度重叠。

因此更严谨的结论是：

> **Exact textual duplication accounts for only a small fraction of observed errors, while semantic redundancy may still contribute to retrieval ambiguity.**

---

# 5. 大量错误实际上发生在正确类别内部

错误组中：

- rank 2~5：Top1 同类别率 76.80%
- rank 6~10：77.75%
- rank >10：67.64%

这说明大量错误并不是：

```text
church → airport
beach  → forest
```

这种完全错误。

更典型的是：

```text
church_59 → church_58
beach_44  → beach_400
```

因此：

\[
\boxed{
\text{CLIP 已经进入正确 coarse semantic neighborhood，}
\quad
\text{但无法在 neighborhood 内精确完成 instance ranking。}
}
\]

可以概括为：

```text
Caption
   ↓
Correct scene/category neighborhood          ✓
   ↓
Intra-category ranking
   ↓
Exact GT image                               ✗
```

这就是当前最重要的错误结构。

---

# 6. Same-category-only Retrieval：关键证据

进一步进行 same-category-only retrieval：

| Setting | R@1 | R@5 | R@10 | Median Rank |
|---|---:|---:|---:|---:|
| Global retrieval | 11.36 | 34.78 | 50.58 | 10 |
| Same-category only | 14.02 | 41.99 | 60.07 | 7 |

同时：

\[
\boxed{\text{Global Top1 category accuracy}=80.33\%}
\]

Same-category candidate 数量中位数：

\[
\boxed{39}
\]

这意味着：

> 即使直接告诉模型正确类别，并且只允许它从正确类别内部寻找 GT，R@1 仍然只有 14.02%。

因此可以比较明确地得到：

\[
\boxed{
\text{The bottleneck lies in intra-category discrimination rather than coarse scene recognition.}
}
\]

即：

\[
\boxed{
\text{当前瓶颈主要位于 Scene Recognition 之后。}
}
\]

---

# 7. Caption 信息粒度与检索难度

按 GT rank 分组：

| GT rank | 平均长度 | Entity 数 | Attribute 数 | Relation 数 |
|---|---:|---:|---:|---:|
| rank = 1 | 12.76 | 2.12 | 0.84 | 0.67 |
| rank = 2~5 | 11.69 | 1.77 | 0.81 | 0.55 |
| rank = 6~10 | 11.24 | 1.60 | 0.79 | 0.49 |
| rank >10 | 10.95 | 1.54 | 0.74 | 0.52 |

其中 Entity 数量呈现较清晰趋势：

\[
2.12
\rightarrow
1.77
\rightarrow
1.60
\rightarrow
1.54
\]

Caption length 也整体随 rank 恶化下降。

这说明：

> **具有更多判别性语义信息的 caption，通常更容易与具体实例建立准确绑定。**

但不能简单总结成：

> “caption 越长，检索越好。”

真正关键的是：

\[
\boxed{
\text{Discriminative Semantic Information}
}
\]

包括：

- Entity
- Attribute
- Relation
- Entity configuration
- Spatial structure

例如：

```text
many buildings beside a road
```

虽然可能比较长，但仍然很泛。

而：

```text
a circular storage tank beside two rectangular buildings
```

虽然未必更长，却具有明显更强的实例辨识能力。

---

# 8. Entity / Attribute / Relation Consistency Diagnostic

需要明确：

> 当前 consistency diagnostic 是基于 annotation caption 构造的 proxy，不是视觉 VQA，也没有 region-level 人工标注。

因此它主要用于：

- Motivation；
- Error diagnosis；
- Method design；

不能直接视为严格视觉事实。

---

## 8.1 全部错误 pair

| Component | GT | Wrong Top1 | Gap |
|---|---:|---:|---:|
| Entity | 93.00 | 78.10 | 14.90 |
| Attribute | 75.37 | 42.99 | 32.39 |
| Relation | 77.25 | 38.29 | 38.96 |

---

## 8.2 Same-category 错误

| Component | GT | Wrong Top1 | Gap |
|---|---:|---:|---:|
| Entity | 92.96 | 82.67 | 10.29 |
| Attribute | 73.13 | 44.42 | 28.71 |
| Relation | 74.61 | 41.76 | 32.84 |

结果非常重要：

Wrong Top1 往往已经保留主要实体：

```text
church
road
trees
buildings
```

因此 Entity gap 相对较小：

\[
10.29
\]

真正更大的差异来自：

### Attribute

```text
large / small
dense / sparse
circular / rectangular
few / many
```

### Relation

```text
beside
surrounded by
crossing
between
next to
behind
```

Relation gap 达到：

\[
\boxed{32.84}
\]

因此：

\[
\boxed{
\text{Entity presence alone is insufficient for intra-category discrimination.}
}
\]

---

# 9. 当前论文真正的出发点

最开始较宽泛的说法：

> 遥感图文存在 semantic gap，需要更细粒度的跨模态对齐。

现在应该升级为：

> **现有视觉语言模型已经能够较好地捕获遥感图文的粗粒度场景语义，但遥感图像在同类别内部具有高度相似的全局外观，使得精确实例检索仍然困难。我们的诊断表明，大量错误 Top-1 实际仍处于正确语义类别中，并且通常保留主要实体，而真正显著的差异集中在实体属性及空间关系。因此，遥感图文检索的关键瓶颈已经从粗粒度跨模态识别转向结构化的类内细粒度辨识。**

推荐英文：

> **Our diagnostic analysis reveals that the primary bottleneck of remote-sensing image-text retrieval lies in intra-category discrimination rather than coarse scene recognition. Although erroneous top-ranked images often preserve the major scene entities, they exhibit substantially larger inconsistencies in entity attributes and spatial relations. This suggests that global image-text alignment, or even isolated entity matching, is insufficient for distinguishing visually similar remote-sensing instances, motivating structured fine-grained grounding that jointly models entities, attributes, and their relational configurations.**

核心概念应该逐渐从：

```text
Global-local alignment
Entity alignment
```

转向：

\[
\boxed{
\text{Structured Fine-grained Grounding}
}
\]

即：

\[
\boxed{
\text{Entity}
+
\text{Attribute}
+
\text{Relation}
}
\]

---

# 10. 对原始方法框架的重新理解

原始整体方案：

\[
\boxed{
\text{CLIP Global}
+
\text{LLM Entity Grounding}
+
\text{Hierarchical Prototype Learning}
+
\text{GMM Fine Prototype Refinement}
+
\text{Prototype OT}
+
\text{Token/Patch Fine-grained Alignment}
}
\]

整体主骨架仍然可以保留，但各模块的**角色必须重新定义**。

新的核心逻辑应该是：

\[
\boxed{
\text{Global CLIP}
\rightarrow
\text{Structured Grounding}
\rightarrow
\text{Intra-category Hierarchy}
\rightarrow
\text{Fine Semantic Modes}
\rightarrow
\text{Cross-modal Structural Matching}
}
\]

而不是简单的模块堆叠。

---

# 11. 第一调整：Entity Grounding → Structured Semantic Grounding

原计划：

\[
\text{LLM Entity Extraction}
\rightarrow
\text{Entity Grounding}
\]

现在应扩展成：

\[
\boxed{
\text{Structured Semantic Grounding}
=
\text{Entity}
+
\text{Attribute}
+
\text{Relation}
}
\]

例如 caption：

```text
a circular storage tank beside two rectangular buildings
```

不能只提取：

```text
Entity:
- storage tank
- buildings
```

还应该保留：

```text
Entity:
- storage tank
- buildings

Attribute:
- tank: circular
- buildings: rectangular
- buildings: two

Relation:
- storage tank beside buildings
```

因此后续第一创新模块真正需要回答的问题是：

\[
\boxed{
\text{如何将结构化文本语义单位与视觉局部区域建立对应？}
}
\]

---

# 12. 第二调整：Hierarchical Prototype 必须服务于类内结构

不能只建立：

```text
airport prototype
church prototype
beach prototype
farmland prototype
```

因为当前诊断已经说明：

\[
\boxed{
\text{CLIP 对 coarse scene category 已经相对较强。}
}
\]

如果 prototype 只是重新学习大类，很可能重复 CLIP 已有能力。

后续 prototype 应该更接近：

```text
Church
│
├── isolated church
├── church + dense trees
├── church + surrounding buildings
├── church beside road
└── church at intersection
```

或者：

```text
Airport
│
├── parallel runways
├── single runway + terminal
├── runway + dense aircraft
├── runway + sparse buildings
└── crossing runway configuration
```

因此：

\[
\boxed{
\text{Coarse Prototype}
=
\text{Scene / Semantic Neighborhood}
}
\]

而：

\[
\boxed{
\text{Fine Prototype}
=
\text{Attribute-Relation Conditioned Intra-class Mode}
}
\]

---

# 13. 第三调整：GMM 不应该提前锁死

原始设计中的：

\[
\text{GMM Fine Prototype Refinement}
\]

仍然有理论合理性：

> 同一个 coarse category 内部可能存在多个稳定子分布。

例如：

```text
church + trees
church + road
church + dense buildings
isolated church
```

但是现在不应该直接认为：

\[
\boxed{\text{GMM 必须存在}}
\]

需要比较：

- Single prototype
- K-means sub-prototypes
- Learnable sub-prototypes
- Prototype memory
- GMM sub-prototypes

例如，如果最终：

```text
Single prototype     35.2
K-means              35.8
GMM                  35.9
```

那么 GMM 没有足够必要性。

只有出现类似：

```text
Single prototype     35.2
GMM                  37.0
```

才说明：

\[
\boxed{\text{GMM 确实捕获了重要的多模态类内结构}}
\]

因此当前原则是：

\[
\boxed{
\text{复杂模块必须对应已经观察到的明确错误，并通过消融证明价值。}
}
\]

---

# 14. 第四调整：OT 必须服务于结构关系，而不是作为数学装饰

假设 GT 和 Wrong Top1 都包含：

```text
church
road
trees
buildings
```

仅做 Entity presence：

```text
GT       ✓ ✓ ✓ ✓
Wrong    ✓ ✓ ✓ ✓
```

无法区分。

真正差异可能是：

```text
GT:
church surrounded by trees
road beside church
buildings behind church

Wrong:
church beside trees
road crossing buildings
```

因此需要解决的是：

\[
\boxed{
\text{相同 semantic components 下的不同结构配置}
}
\]

OT 的合理定位应该是：

\[
\boxed{
\text{text structured semantic units}
\leftrightarrow
\text{visual local semantic units}
}
\]

之间的：

- soft correspondence；
- many-to-many matching；
- structural transport；
- local semantic assignment。

如果 OT 最终无法帮助：

- Entity correspondence；
- Attribute-conditioned matching；
- Relation/configuration preservation；

那么就需要重新评估 OT 是否真的有必要。

---

# 15. 最难类别对方法设计的启发

当前 within-category 最难类别包括：

- farmland
- bareland
- parking
- airport
- bridge
- beach

这些类别具有共同特点：

\[
\boxed{
\text{同类别内部 global appearance 高度相似}
}
\]

真正能区分实例的往往不是 scene label，而是局部实体、属性和空间结构。

---

## 15.1 Farmland

常见：

```text
plots
roads
vegetation
water
```

实例差异更多来自：

```text
plot configuration
orientation
water-road relation
```

---

## 15.2 Airport

常见：

```text
runway
terminal
apron
aircraft
```

实例差异来自：

```text
runway number
orientation
terminal position
aircraft distribution
```

---

## 15.3 Bridge

常见：

```text
bridge
river
road
```

实例差异来自：

```text
bridge direction
river direction
crossing relationship
surrounding buildings
```

这进一步支持：

\[
\boxed{
\text{Entity alone is not enough; configuration matters.}
}
\]

---

# 16. 当前最优先任务：先修正 Multi-positive

在正式进入创新模块前，应该首先完成：

\[
\boxed{
\text{Multi-positive CLIP Correction}
}
\]

原因：

RSICD 一张图有 5 条 caption。

对于同一图像：

```text
Image A
├── Caption A1
├── Caption A2
├── Caption A3
├── Caption A4
└── Caption A5
```

如果 batch 内同时出现：

```text
Image A + Caption A1
Image A + Caption A2
```

当前 diagonal CLIP 会产生监督冲突：

```text
A1 ↔ Image A    positive
A2 ↔ Image A    positive in reality

but

A1 considers A2 negative
A2 considers A1 negative
```

因此，后续主方法不应该继续建立在一个已经明确存在 false-negative bias 的训练基础上。

---

# 17. Multi-positive 的严格定义

只根据：

\[
\boxed{\text{Same Image ID}}
\]

构造 positive。

正样本 mask：

\[
M_{ij}
=
\mathbf{1}(id_i=id_j)
\]

归一化：

\[
Q_{ij}
=
\frac{M_{ij}}
{\sum_jM_{ij}}
\]

I2T：

\[
L_{I2T}
=
-\frac1B
\sum_i\sum_j
Q_{ij}
\log
\frac{\exp(s_{ij})}
{\sum_k\exp(s_{ik})}
\]

T2I 同理：

\[
L=
\frac{L_{I2T}+L_{T2I}}{2}
\]

如果 batch 内没有重复 image ID：

\[
M=I
\]

则自动退化为：

\[
\boxed{\text{原始 diagonal CLIP loss}}
\]

因此 Multi-positive 不是重新定义检索任务，而只是：

> **当 batch 内真实存在多个合法 positive 时，不再错误地把它们当 negative。**

---

# 18. Multi-positive 的重要边界

必须保持：

```text
same image_id
    ↓
positive
```

而：

```text
different image
even same category
    ↓
negative
```

例如：

```text
beach_44
beach_400
```

即使都是 beach：

\[
\boxed{\text{仍然必须互为 negative}}
\]

原因是后续论文真正要解决的正是：

\[
\boxed{
\text{Same-category Different-instance Discrimination}
}
\]

如果把同类别不同图像全部当 positive，会直接破坏研究目标。

---

# 19. Multi-positive 的科研定位

Multi-positive 很重要，但不是论文主要创新。

它解决的是：

\[
\boxed{
\text{Supervision Consistency}
+
\text{False-negative Correction}
}
\]

而不是：

\[
\boxed{
\text{Structured Intra-category Fine-grained Discrimination}
}
\]

因此实验体系应保留：

| Model | Role |
|---|---|
| Zero-shot CLIP | 原始迁移能力 |
| CLIP Full-FT Diagonal | Vanilla baseline |
| CLIP Full-FT Multi-positive | Corrected / stronger baseline |
| + Structured Grounding | Proposed module |
| + Hierarchical Prototype | Proposed module |
| + Fine Sub-prototype | Proposed module |
| + OT / Local Alignment | Proposed module |
| Full Method | Final model |

这样后续每个模块的增益才能清楚归因。

---

# 20. 推荐后续任务顺序

## Stage 1–8：Vanilla CLIP Baseline

已完成。

\[
\boxed{\text{CLIP-Diagonal Test mR}=32.97}
\]

---

## Stage 9：Multi-positive CLIP Correction

只修改：

- Dataset 返回 image_id；
- Loss 使用 image-id multi-positive target；
- Trainer 将 image_id 传给 loss；
- 增加对应 sanity tests。

其他保持一致：

- split；
- backbone；
- pretrained；
- batch size；
- optimizer；
- learning rate；
- epochs；
- scheduler；
- Validation checkpoint selection。

目的：

\[
\boxed{
\text{单独测量 false-negative correction 的贡献}
}
\]

---

## Stage 10：Structured Semantic Grounding

从单纯 Entity 扩展为：

\[
\boxed{
\text{Entity}
+
\text{Attribute}
+
\text{Relation}
}
\]

第一版应该保持最小化。

不要同时加入：

- Prototype；
- GMM；
- OT。

目标：

> 验证 Structured Semantics 本身是否能够改善 intra-category ranking。

重点观察：

- T2I R@1；
- Same-category R@1；
- Hard-neighbor ranking。

---

## Stage 11：Hierarchical Prototype

目标：

\[
\boxed{
\text{Coarse Semantic Neighborhood}
\rightarrow
\text{Fine Intra-category Modes}
}
\]

验证是否真正改善：

- same-category R@1；
- same-category Median Rank；
- T2I R@1；
- 难类表现。

---

## Stage 12：Fine Sub-prototype

先验证：

\[
\boxed{
\text{一个 coarse prototype 内是否真的需要多个子模式}
}
\]

候选：

- Single prototype；
- K-means；
- Learnable prototype；
- GMM。

只有 GMM 有显著独立收益时，才把 GMM 保留为核心方法。

---

## Stage 13：Prototype-level OT

目标：

\[
\boxed{
\text{Structured Cross-modal Correspondence}
}
\]

重点验证：

- Entity correspondence；
- Attribute-aware matching；
- Relation/configuration matching；
- many-to-many fine prototype correspondence。

---

## Stage 14：Token/Patch Fine-grained Alignment

最终将 prototype-level structured semantics 下沉至：

\[
\text{Text token / phrase}
\leftrightarrow
\text{Visual patch / region}
\]

解决最细粒度实例排序。

---

# 21. 最终理想的 Ablation 路线

```text
CLIP-Diag                     32.97
CLIP-MultiPos                 xx.xx
+ Structured Grounding        xx.xx
+ Hierarchical Prototype      xx.xx
+ Fine Sub-prototype          xx.xx
+ Prototype OT                xx.xx
+ Token/Patch Alignment       xx.xx
Full Method                   xx.xx
```

每一步都对应一个明确问题。

---

## Multi-positive

回答：

> False-negative correction 有多少贡献？

---

## Structured Grounding

回答：

> Entity / Attribute / Relation 是否改善实例辨识？

---

## Hierarchical Prototype

回答：

> 类内子结构建模是否有效？

---

## Fine Sub-prototype

回答：

> 类内是否存在值得显式建模的多模态语义结构？

---

## OT

回答：

> 跨模态 structured correspondence 是否具有独立价值？

---

## Token/Patch Alignment

回答：

> 更低层的局部细粒度监督是否进一步提升 exact instance ranking？

---

# 22. 当前完整 Motivation Chain

现在已经形成一条较完整的证据链：

```text
CLIP Global Baseline
        ↓
Coarse scene semantics already reasonably strong
        ↓
Global Top1 category accuracy = 80.33%
        ↓
Exact T2I R@1 = 11.36%
        ↓
Same-category-only R@1 = 14.02%
        ↓
Main bottleneck:
Intra-category Instance Discrimination
        ↓
Wrong Top1 often preserves major entities
        ↓
Attribute / Relation gaps are much larger
        ↓
Need:
Structured Fine-grained Grounding
        ↓
Entity + Attribute + Relation
        ↓
Hierarchical Intra-category Prototype
        ↓
Fine Semantic Modes
        ↓
Structured Cross-modal Correspondence
        ↓
Token/Patch Fine-grained Alignment
        ↓
Exact Instance Retrieval
```

可以浓缩成：

\[
\boxed{
\textbf{从粗粒度语义识别，转向结构化类内细粒度辨识。}
}
\]

---

# 23. 科研边界

## 23.1 不再用 Test 调参

Test 可以继续作为：

- Error analysis；
- Case study；
- Motivation diagnostic；

但以下参数必须由 Validation 决定：

- epoch；
- learning rate；
- loss weight；
- prototype number；
- Top-K；
- GMM component number；
- OT regularization；
- margin；
- threshold。

训练流程始终保持：

```text
Train
  ↓
Validation selection
  ↓
Test once
```

---

## 23.2 Consistency 结果必须明确是 Proxy

当前 Entity / Attribute / Relation consistency：

\[
\boxed{
\text{是基于 caption annotation 的诊断代理指标}
}
\]

不是：

- visual VQA；
- region-level ground truth；
- human verified visual relation annotation。

因此论文中应该称为：

> diagnostic proxy / consistency proxy

不能直接作为严格视觉事实。

---

## 23.3 不为了复杂而复杂

后续每个模块必须满足：

\[
\boxed{
\text{Observed Error}
\rightarrow
\text{Method Component}
\rightarrow
\text{Controlled Improvement}
}
\]

例如：

```text
Same-category confusion
    ↓
Hierarchical fine prototype
```

```text
Attribute / Relation discrepancy
    ↓
Structured semantic grounding
```

```text
Structured component correspondence
    ↓
OT / Local alignment
```

任何无法对应明确问题、或消融中没有独立贡献的模块，都应该考虑删除。

---

# 24. 当前对整个项目的最终认识

整个项目的思路已经从最开始的：

\[
\boxed{
\text{“设计一个大而全的细粒度模型”}
}
\]

逐步转变为：

\[
\boxed{
\text{“针对 CLIP 已经暴露出来的类内细粒度排序瓶颈，}
\quad
\text{逐层补充真正缺失的结构语义能力。”}
}
\]

当前最明确的后续任务是：

\[
\boxed{
\textbf{先完成 Multi-positive Corrected CLIP Baseline。}
}
\]

在此之后，第一创新模块不再应是狭义：

\[
\text{Entity Grounding}
\]

而应该围绕：

\[
\boxed{
\text{Entity–Attribute–Relation Structured Grounding}
}
\]

重新设计。

后续：

- Hierarchical Prototype；
- Fine Sub-prototype；
- GMM；
- OT；
- Token/Patch Alignment；

都必须围绕：

\[
\boxed{
\text{Intra-category Fine-grained Instance Discrimination}
}
\]

这一核心问题展开。

---

# 25. 当前一句话论文主线

中文：

> **现有 CLIP 已经能够找到正确的遥感场景语义邻域，但难以在大量视觉高度相似的同类候选中利用实体属性与空间关系精确辨识目标实例，因此需要面向类内结构差异的层级化、结构化细粒度跨模态对齐。**

英文：

> **Existing CLIP-based models can already identify coarse remote-sensing semantic neighborhoods, but struggle to distinguish the exact target instance among visually similar intra-category candidates. We therefore focus on structured fine-grained cross-modal alignment that exploits entities, attributes, and spatial relations to improve intra-category discrimination.**

---

# 26. 当前版本的方法主骨架

基于当前诊断后的推荐框架：

\[
\boxed{
\text{CLIP Global}
+
\text{Multi-positive Global Supervision}
+
\text{Structured Semantic Grounding}
+
\text{Hierarchical Intra-category Prototype Learning}
+
\text{Fine Sub-prototype Modeling}
+
\text{Structured Prototype OT}
+
\text{Token/Patch Fine-grained Alignment}
}
\]

其中需要特别区分：

### 基础训练纠偏

\[
\boxed{
\text{Multi-positive CLIP}
}
\]

作用：

> 消除同一图像多 caption 的 batch 内 false-negative supervision。

---

### 核心创新主线

\[
\boxed{
\text{Structured Grounding}
\rightarrow
\text{Hierarchical Intra-category Modeling}
\rightarrow
\text{Structured Cross-modal Alignment}
}
\]

作用：

> 解决 coarse semantics 已经正确之后，真正剩余的同类细粒度实例辨识问题。

---

# 27. 当前阶段下一步

当前不建议继续增加新的诊断脚本。

下一步按以下顺序执行：

```text
1. 保留 CLIP-Diag baseline = 32.97
2. 实现 image-id Multi-positive CLIP
3. 完成 loss sanity check
4. 跑 Multi-positive Full-FT
5. Validation 选 best
6. Test once
7. 对比：
   - mR
   - I2T
   - T2I
   - T2I R@1
   - Same-category R@1
8. 锁定 Corrected Strong Baseline
9. 开始 Structured Entity-Attribute-Relation Grounding
```

只有在 Multi-positive corrected baseline 锁定以后，才正式进入论文方法模块开发。
