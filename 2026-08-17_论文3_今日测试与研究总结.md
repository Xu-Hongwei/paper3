# 2026-08-17 论文3：今日测试与研究总结

## 1. 今日研究主线

今天的核心目标，是继续解决论文3中已经暴露出的关键瓶颈：

> **文本侧 Entity / Entity+Attribute 已经具备较明确的细粒度语义结构，但视觉侧 Region / Prototype 仍受到尺度、背景和场景上下文污染，导致 Text–Visual fine prototype correspondence 很弱。**

因此今天没有继续推进 GMM / OT，而是优先回答两个基础问题：

1. **视觉实体区域能否比原来的多尺度 Rectangle 更准确、更 object-centric？**
2. **如果视觉区域选得更干净，Text–Visual fine prototype alignment 是否会自然改善？**

今天依次测试了：

```text
Manual Box → SAM2
CLIP Top Region → SAM2
CLIP Spatial Heatmap → SAM2
CLIP Spatial Points → SAM2
Grounding DINO
Grounding DINO → SAM2
Grounding DINO Teacher → CLIP Visual Prototype
Raw GDINO vs Object-filtered GDINO
```

最终结论已经比较明确：

\[
\boxed{
\text{Better Grounding}
\rightarrow
\text{Better Visual Structure}
}
\]

但：

\[
\boxed{
\text{Better Visual Structure}
\nRightarrow
\text{Automatic Fine Cross-modal Alignment}
}
\]

也就是说，**Grounding Quality 是瓶颈之一，但真正尚未解决的问题已经进一步收敛到 Fine-semantic Alignment。**

---

# 2. C0.4：CLIP → SAM2 路线测试

## 2.1 Manual Box → SAM2

首先使用人工 box 检查 SAM2 自身是否适用于遥感实体区域精细化。

### Aircraft

人工框覆盖完整飞机后：

- SAM 能生成干净的飞机 mask；
- 多个 proposal 中存在明显正确的 object mask；
- 说明 SAM2 对 compact object 的边界恢复能力良好。

结论：

\[
\boxed{\text{Aircraft Human Box → SAM2：PASS}}
\]

### River

人工框覆盖上方完整 curved river 后：

- SAM 能恢复连续河道；
- 某些 mask 能明显区分河水与周围城市/土地；
- 虽然 SAM score 最高的 proposal 不一定是人工认为最好的，但 proposal 集中确实存在高质量河流 mask。

结论：

\[
\boxed{\text{River Human Box → SAM2：PASS}}
\]

因此可以确认：

> **SAM2 本身不是当前主要瓶颈，真正困难的是如何自动生成可靠 prompt。**

---

## 2.2 CLIP Top Region → SAM2

使用原有 32/64/96/128 多尺度 Rectangle，共 230 个 Region。

River 样本：

```text
caption:
many small buildings are in two sides of a curved river

coarse query:
river
```

CLIP Top-3 Rectangle：

```text
Box1: 32px  [0,80,32,112]      score=0.2494
Box2: 128px [64,64,192,192]    score=0.2465
Box3: 32px  [160,80,192,112]   score=0.2443
```

最终 CLIP+SAM 选择的是一个左侧局部河段，而不是完整 curved river。

### 结论

核心问题是：

\[
\boxed{
\text{Semantic Purity}
\neq
\text{Object Completeness}
}
\]

CLIP 很容易认为“小而纯的水体 patch”比“完整但包含更多上下文的河流区域”更像 `"river"`。

因此：

\[
\boxed{\text{Top-score Rectangle ≠ object-level localization}}
\]

---

## 2.3 CLIP Spatial Heatmap → SAM2

为解决 Top Region 太局部的问题，尝试：

```text
230 Regions
→ 各尺度 score 标准化
→ score 投回 224×224 空间
→ spatial heatmap
→ threshold + dilation
→ connected component
→ aggregated box
→ SAM2
```

River 自动产生 coarse box：

```text
[1, 0, 111, 218]
```

实际几乎覆盖左半幅整条纵向区域，并没有恢复人工框那种横向 curved river 范围。

### 失败原因

Region score 本质是：

\[
s_k=\cos(t,r_k)
\]

但将其投回矩形内部相当于假设：

\[
\forall (x,y)\in R_k,\quad H(x,y)=s_k
\]

即默认：

> 一个高分 Region 内部所有像素都具有该 Entity 语义。

这个假设不成立。

一个 128×128 crop 可能只是：

```text
20% river + 80% city/land
```

但整块都会被赋高 response，因此最后得到的是矩形结构叠加，而不是真实 river shape。

结论：

\[
\boxed{\text{Rectangle score projection ≠ true pixel semantic heatmap}}
\]

该路线停止，不继续调 quantile / dilation / margin。

---

## 2.4 CLIP Spatial Peaks → SAM Multi-Point

进一步尝试不把 Region score解释为整块像素证据，而只把高分 Region 的中心作为 SAM positive points。

River 自动选出的三个点：

```text
P1 = (16, 96)   score=0.2494   32px
P2 = (176, 96)  score=0.2443   32px
P3 = (64, 32)   score=0.2413   64px
```

分别测试：

```text
1 positive point
2 positive points
3 positive points
```

结果仍未得到稳定完整 river mask。

### 关键问题

高分 crop 只说明：

> crop 中存在与 `"river"` 有关的语义。

但：

\[
\boxed{
\text{High-score Crop Center}
\not\Rightarrow
\text{True Semantic Location}
}
\]

特别是 64px crop 中，真正 river evidence 可能位于 crop 边缘，而中心点落在城市或土地。

同时，CLIP 对 SAM mask 的 rerank 也出现问题：

- 1 point 最佳 MaskCLIP 对应约 69% 整图面积；
- 2 points 最佳 MaskCLIP 对应约 61% 整图面积；
- 3 points 最佳 MaskCLIP 对应约 54% 整图面积。

说明：

\[
\boxed{
\text{High CLIP Mask Similarity}
\not\Rightarrow
\text{Good Spatial Completeness / Purity}
}
\]

### C0.4 总结

目前已经形成较清楚证据链：

| 方法 | Aircraft | River | 结论 |
|---|---:|---:|---|
| Human Box → SAM2 | PASS | PASS | SAM 本身可用 |
| CLIP Top Region → SAM2 | 可局部工作 | FAIL | CLIP Top Region 不保证完整目标 |
| CLIP Heatmap → SAM2 | — | FAIL | Rectangle score 不能直接视为 pixel evidence |
| CLIP Points → SAM2 | — | FAIL | Region center 不是可靠 semantic point |

因此暂停继续修补 CLIP→SAM prompt heuristic。

---

# 3. Grounding DINO 引入与理解

## 3.1 Grounding DINO 的角色

Grounding DINO 是一个文本条件开放词汇目标检测器：

\[
\boxed{
\text{Text Query}
+
\text{Image}
\rightarrow
\text{Object Boxes}
}
\]

例如：

```text
"airplane"
→ airplane box1
→ airplane box2
→ airplane box3
```

它和 CLIP / SAM2 的职责不同：

```text
Grounding DINO
→ Where is the entity?

SAM2
→ Which pixels belong to it?

CLIP
→ How to represent it in joint image-text space?
```

需要特别区分：

- Grounding DINO 中的 DINO 是 DETR/DINO detector 路线；
- 不是之前讨论的 DINOv2 自监督视觉表示模型。

---

# 4. C0.5A：Grounding DINO Coarse Localization

## 4.1 River

输入：

```text
query = "river"
```

返回：

```text
#1 score=0.4202
box ≈ [0,0,224,224]

#2 score=0.3674
box ≈ [0,25,224,224]
```

两个框几乎都是整图。

### 判断

如果把 Grounding DINO 当作精确 object detector：

\[
\boxed{\text{River：FAIL}}
\]

但后来重新审视后认为：

> 对原型学习而言，不一定要求 bounding box 精确，只要 proposal 包含足够 Entity semantic evidence，就可能仍然可用。

因此 River 不能仅靠“框是否漂亮”判死刑，最终应看 prototype utility。

---

## 4.2 Aircraft

输入：

```text
dataset_index = 1570
query = "airplane"
```

Grounding DINO Tiny 返回 3 个 detection：

```text
#1 score=0.6846
box=[35.3,179.5,85.6,219.0]

#2 score=0.6391
box=[0.0,0.0,76.0,35.0]

#3 score=0.6511
box=[0.0,41.7,86.6,115.6]
```

三个框都确实对应飞机实例。

结论：

\[
\boxed{\text{Aircraft Grounding DINO：PASS}}
\]

说明 Grounding DINO 对 compact object-like entity 有较强定位能力。

---

# 5. C0.5B：Grounding DINO → SAM2

继续对 Aircraft 的 3 个 Grounding DINO boxes 使用 SAM2。

结果：

- Box1：SAM proposals 基本只覆盖底部小飞机；
- Box2：M1/M2 都能干净覆盖主体大飞机；
- Box3：即使飞机被图像边界裁切，SAM 仍能沿可见机体分割。

典型结果：

```text
GDINO Box2 score ≈ 0.651
SAM M1 score ≈ 0.895
SAM M2 score ≈ 0.891
mask area ≈ 0.038
```

结论：

\[
\boxed{\text{Aircraft Grounding DINO → SAM2：Strong PASS}}
\]

但随后重新考虑论文目标后认为：

> 如果目的只是建立视觉原型，Grounding DINO box 本身已经可能足够，不一定必须增加 SAM。

因此 SAM 暂时停止作为主线模块。

---

# 6. 一个关键方法论问题：外部能力还是自身能力？

今天提出了一个非常重要的问题：

> 如果最终推理阶段一直调用 Grounding DINO，那么性能提升到底来自我们的 RSITR 方法，还是来自一个本身已经很强的外部 grounding foundation model？

这是一个核心论文风险。

因此 Grounding DINO 的角色进行了调整：

## 不推荐

```text
Inference:
Entity
→ Grounding DINO
→ Region
→ Retrieval
```

因为这样很容易变成：

\[
\boxed{\text{External Capability Injection}}
\]

而不是模型自身能力提升。

## 更合理定位

\[
\boxed{\text{Grounding DINO = Teacher / Oracle Diagnostic}}
\]

用于：

1. 判断“如果视觉区域真的更好，prototype 是否有潜力”；
2. 后续可能只在训练期生成 pseudo grounding；
3. 最终 inference 阶段 Grounding DINO 退出。

理想路线：

```text
Training only:
Entity
→ Grounding DINO
→ pseudo region / pseudo supervision
→ teach CLIP local representation

Inference:
Image + Text
→ CLIP-based retrieval model only
```

即：

\[
\boxed{
\text{External Knowledge}
\rightarrow
\text{Internalized Capability}
}
\]

而不是把外部模型永久挂在检索系统上。

---

# 7. GDINO Teacher Prototype Feasibility

为了验证：

> 如果视觉区域由更强的外部 grounding teacher 提供，视觉原型结构是否明显改善？

构建了：

```text
Coarse Entity
→ Grounding DINO
→ Top-K Boxes
→ CLIP Vision
→ Visual Sample Feature
```

视觉选择阶段只使用 coarse query：

```text
aircraft → "airplane"
building → "building"
river    → "river"
stadium  → "stadium"
```

没有使用 fine attribute。

文本侧仍然使用：

\[
\boxed{
\text{Entity + color/size/shape/state}
}
\]

因此避免：

\[
\text{fine text}
\rightarrow
\text{fine visual selection}
\]

造成 circularity。

多实例第一版使用：

\[
v_i=
\operatorname{Norm}
\left(
\frac{1}{M}
\sum_{m=1}^{M}v_{im}
\right)
\]

同时缓存中保留所有 instance features，便于后续分析。

---

# 8. Aircraft GDINO Teacher Prototype 结果

120 个 Aircraft occurrence：

```text
Grounded samples: 112 / 120
Coverage: 93.3%
Average boxes: 2.52
```

说明 Grounding DINO 对 Aircraft 并非只在少量简单样本上有效。

## 数值结果

### K=1

```text
T-comp = 0.909
V-comp = 0.719
ProtoCos = 0.347
```

相比旧 Rectangle：

```text
V-comp ≈ 0.850
```

GDINO 后 K=1 compactness 反而下降。

这并不一定是坏事，因为旧 Rectangle 中大量 airport/apron background 是共同成分，导致所有 visual samples 被人为挤在一起。

GDINO 后飞机实例多样性被释放出来。

---

### K=2

```text
T-sil = 0.630
V-sil = 0.499
T-comp = 0.949
V-comp = 0.820
ProtoCos = 0.306
PairCons = 0.518
MargChance = 0.500
```

旧 Rectangle Aircraft K=2：

```text
V-sil ≈ 0.206
ProtoCos ≈ 0.323
PairCons ≈ 0.475
```

视觉 silhouette：

\[
0.206\rightarrow0.499
\]

明显提高。

---

### K=4

GDINO：

```text
T-sil = 0.695
V-sil = 0.235
T-comp = 0.965
V-comp = 0.850
ProtoCos = 0.301
PairCons = 0.259
MargChance = 0.215
```

旧 Rectangle：

```text
V-sil ≈ 0.119
ProtoCos ≈ 0.330
PairCons ≈ 0.258
```

因此：

\[
V\text{-sil}:0.119\rightarrow0.235
\]

明显提高。

但：

\[
ProtoCos:0.330\rightarrow0.301
\]

反而下降。

PairCons：

\[
0.258\rightarrow0.259
\]

几乎不变。

真正超出随机 cluster-size baseline 的部分仅：

\[
0.259-0.215=0.044
\]

---

### K=8

```text
T-sil = 0.771
V-sil = 0.193
ProtoCos = 0.294
PairCons = 0.205
MargChance = 0.163
```

旧 Rectangle：

```text
V-sil ≈ 0.112
ProtoCos ≈ 0.317
PairCons ≈ 0.108
```

视觉结构仍提高，但 prototype cosine 仍没有改善。

---

# 9. Montage / Prototype Matrix 诊断

数字之外，K=4 / K=8 montage 和 prototype matrix 给出了更重要的信息。

## 9.1 Visual clusters 主要在按 proposal granularity 聚类

K=4 montage 中发现：

### C0

大量：

```text
box area ≈ 0.97 ~ 1.00
```

内容实际上是：

```text
whole airport / runway / scene-level crop
```

### C1

更多：

```text
small / blurred / partial aircraft
```

### C2

大量：

```text
large contextual airport / terminal / apron regions
box area ≈ 0.83 ~ 1.00
```

### C3

主要是：

```text
tight single-aircraft crops
```

因此：

\[
V\text{-sil}\uparrow
\]

很大一部分并不是因为 fine semantics 变清楚，而是因为：

\[
\boxed{
\text{whole scene}
\leftrightarrow
\text{large context}
\leftrightarrow
\text{partial object}
\leftrightarrow
\text{tight object}
}
\]

本身就是很强的聚类轴。

因此：

\[
\boxed{
\text{Visual Silhouette}\uparrow
\not\Rightarrow
\text{Fine Semantic Structure}\uparrow
}
\]

---

## 9.2 Prototype similarity matrix 仍较平

K=4 典型 matrix：

```text
        V0    V1    V2    V3
T0     .30   .23   .30   .25
T1     .31   .30   .33   .33
T2     .29   .27   .30   .29
T3     .29   .26   .30   .28
```

缺乏清晰：

\[
T_i\rightarrow V_j
\]

选择性。

某些 visual prototype 对几乎所有 Text prototype 都比较 generic-compatible。

K=8 同样出现明显 column preference / prototype hub。

因此：

\[
\boxed{
\text{OT 目前不能解决这个问题}
}
\]

因为：

\[
C_{ij}=1-\cos(T_i,V_j)
\]

本身没有形成有意义的 selective structure。

OT 不能凭空创造语义 correspondence。

---

## 9.3 Joint PCA 仍存在明显 modality gap

Text samples/prototypes 和 Visual samples/prototypes 在联合 PCA 上基本分成两个区域：

```text
Visual                  Text
xxxxxx                 ooooo
xxxxx                  oooo
```

说明即使视觉 crop 更 object-centric：

\[
\boxed{
\text{Local CLIP visual}
\leftrightarrow
\text{Entity+Attribute text}
}
\]

仍然存在明显 modality gap。

因此：

> 找到 object 只是解决 “Where”，并没有自动解决 “Which fine visual variation corresponds to which text semantics”。

---

# 10. GDINO Object Purification Diagnostic

进一步利用已经保存的 GDINO cache，不重新跑 detector。

目的：

> 区分“Grounding proposal purity”问题与真正“Fine Alignment”问题。

面积过滤：

\[
0.002\le A_{box}\le0.35
\]

只保留更 object-like 的 Aircraft proposals。

为了公平，三种模式全部只在同一个 common subset 上比较。

结果：

```text
common = 66 / 112 = 58.9%
```

相对于原始 120：

\[
66/120=55\%
\]

因此：

> 面积硬过滤只能作为 diagnostic，不能直接作为最终 teacher 筛选策略。

---

## 10.1 Raw Mean

同一个 66 sample subset：

```text
paired cosine = 0.2272
```

K=4：

```text
V-sil      = 0.207
ProtoCos   = 0.279
PairCons   = 0.106
Chance     = 0.224
Excess     = -0.118
RowMargin  = 0.014
Hub        = 0.75
```

---

## 10.2 Filtered Top1

取面积有效 proposal 中 GDINO score 最高的一个：

```text
paired cosine = 0.2124
```

K=4：

```text
V-sil      = 0.240
ProtoCos   = 0.282
PairCons   = 0.227
Chance     = 0.229
Excess     = -0.002
RowMargin  = 0.033
Hub        = 1.00
```

### 解读

视觉聚类进一步变清楚：

\[
0.207\rightarrow0.240
\]

但：

\[
PairCons\approx Chance
\]

并且：

\[
Hub=1.00
\]

即所有 Text prototypes 仍然偏好同一个 Visual prototype。

因此：

\[
\boxed{
\text{Cleaner Top1 Object Box}
\not\Rightarrow
\text{Better Fine Cross-modal Correspondence}
}
\]

---

## 10.3 Filtered Mean

把所有面积有效 object proposals 平均：

```text
paired cosine = 0.2201
```

K=4：

```text
V-sil      = 0.187
ProtoCos   = 0.271
PairCons   = 0.288
Chance     = 0.200
Excess     = +0.088
RowMargin  = 0.013
Hub        = 0.50
```

这是目前 K=4 下 cluster correspondence 最好的版本：

\[
Excess=+0.088
\]

Hub 也从：

\[
0.75\rightarrow0.50
\]

但：

```text
RowMargin = 0.013
ProtoCos  = 0.271
Paired cosine = 0.220
```

都没有明显改善。

而且跨 K 不稳定：

```text
K=2  Excess = -0.015
K=4  Excess = +0.088
K=8  Excess = +0.030
```

因此目前只能认为：

> 多实例平均可能保留了一部分 sample-level semantic information，但还不能证明已经形成稳定 fine prototype correspondence。

---

# 11. 今天得到的核心研究结论

## 11.1 Grounding Quality 确实是一个真实瓶颈

从旧 Rectangle：

```text
aircraft + airport context + scale noise
```

到 GDINO：

```text
aircraft-related proposal set
```

Aircraft visual silhouette 显著提高。

因此：

\[
\boxed{
\text{视觉区域质量差确实是原 Prototype 失败的一部分原因}
}
\]

---

## 11.2 Grounding 不是 Fine Alignment 的充分条件

即使 Grounding 更好：

```text
Visual silhouette ↑
```

但：

```text
Prototype cosine ≈ / ↓
PairCons ≈ chance
Prototype matrix 仍较平
```

因此：

\[
\boxed{
\text{Grounding 是必要条件之一，但不是 Fine-grained Alignment 的充分条件}
}
\]

---

## 11.3 Visual 自己最显著的聚类轴 ≠ Text fine semantic axis

文本可能按照：

```text
parked
large
small
multiple
color
state
...
```

形成结构。

视觉则可能优先按照：

```text
orientation
object size
crop completeness
view
background
number of instances
proposal granularity
...
```

形成结构。

因此：

\[
\boxed{
\text{Visual KMeans cluster}
\neq
\text{Text KMeans cluster}
}
\]

并不一定意味着：

> Visual feature 完全没有 fine text semantics。

它只证明：

> Visual 无监督的主变化方向和 Text 无监督的主变化方向不同。

---

## 11.4 当前不应该继续上 GMM / OT

原因已经比较充分：

1. Text/Visual prototype similarity matrix 缺乏稳定 selectivity；
2. prototype hub 明显；
3. PairCons 超随机部分很弱；
4. OT 无法从近似平坦 cost matrix 中创造 fine semantic correspondence。

因此：

\[
\boxed{
\text{Prototype OT 暂缓}
}
\]

等待真正存在可利用的 fine cross-modal structure 后再恢复。

---

## 11.5 Grounding DINO 不应直接作为最终推理增强模块

如果 inference 一直调用 Grounding DINO，那么性能提升很难区分：

```text
Ours improved
```

还是：

```text
External detector is already strong
```

因此 Grounding DINO 目前正式定位为：

\[
\boxed{
\text{Teacher / Oracle / Diagnostic Upper Bound}
}
\]

后续若使用，应优先考虑：

```text
Train:
GDINO pseudo grounding
→ supervise / distill CLIP local branch

Inference:
remove GDINO
→ CLIP-based retrieval only
```

最终目标仍然是：

\[
\boxed{
\text{External Knowledge}
\rightarrow
\text{Internalized CLIP Capability}
}
\]

---

# 12. 当前停止 / 暂缓的路线

今天之后，以下路线暂时不继续投入：

```text
× CLIP Top Rectangle → SAM
× Rectangle score heatmap
× CLIP Region center → SAM points
× 调 SAM 作为主线
× 继续调 GDINO box threshold
× 继续细调 area threshold
× 立即做 GMM
× 立即做 Prototype OT
```

原因：

> 当前主要问题已经不再是“能不能找到一个更漂亮的框”，而是“视觉特征中是否存在、以及如何提取文本所定义的 fine semantic axis”。

---

# 13. 下一步：Visual → Text Fine Cluster 可解码性实验

这是当前最重要的下一步。

## 13.1 核心问题

不再问：

> Text KMeans 和 Visual KMeans 会不会自己独立聚成一样？

而是问：

\[
\boxed{
\text{Text 定义出的 fine semantic cluster}
\text{能否从 Visual feature 中被解码出来？}
}
\]

---

## 13.2 Text Side

使用：

\[
\text{Entity + color/size/shape/state}
\]

得到：

\[
t_i
\]

再使用 Text KMeans：

\[
t_i\rightarrow y_i^{text}
\]

其中：

\[
y_i^{text}\in\{1,\dots,K\}
\]

代表文本定义出的 fine semantic cluster。

---

## 13.3 Visual Side

分别使用：

```text
Raw GDINO Mean
Filtered Top1
Filtered Mean
```

得到视觉 feature：

\[
v_i
\]

但这次**不再对 Visual 做 KMeans**。

直接训练一个很简单的 frozen-feature linear probe：

\[
\hat y_i=Wv_i+b
\]

预测：

\[
y_i^{text}
\]

只训练这一层。

---

## 13.4 必须使用 Group Split

文本侧存在大量重复或近重复 phrase，例如：

```text
planes
parked planes
parked plane
...
```

因此不能随机 sample split。

必须：

\[
\boxed{\text{Group split by fine phrase}}
\]

例如 `"parked planes"` 出现 10 次：

```text
要么全部进入 train
要么全部进入 test
```

不能拆开。

否则 linear probe 结果可能因模板泄漏而虚高。

---

## 13.5 如何解释结果

假设 K=4：

### 情况 A

```text
Random / majority baseline ≈ 25%

Raw visual        28%
Filtered Top1     31%
Filtered Mean     45%
```

说明：

\[
\boxed{
\text{Visual feature 中确实存在 Text fine semantics}
}
\]

只是 Visual KMeans 没有选择这个语义轴。

下一步应重点设计：

\[
\boxed{
\text{Text-conditioned Visual Organization}
}
\]

例如：

- text-conditioned prototype learning；
- fine-semantic projection；
- prototype-guided visual reorganization；
- cross-modal prototype supervision。

---

### 情况 B

```text
Raw             ≈ baseline
Filtered Top1   ≈ baseline
Filtered Mean   ≈ baseline
```

则说明：

\[
\boxed{
\text{即使 region 更干净，当前 CLIP visual feature 仍缺乏所需 fine semantics}
}
\]

此时下一步才应该进入：

```text
Grounding teacher supervision
Local visual adaptation
Attribute-aware local encoder
Grounding-to-CLIP distillation
```

也就是提升 CLIP 自身 local representation。

---

# 14. 当前研究问题已经收敛为

今天研究前，问题更像：

\[
\boxed{
\text{为什么 Prototype Learning 没效果？}
}
\]

经过今天一系列实验后，可以进一步拆成：

\[
\boxed{
\text{Level A：视觉区域是否足够 Entity-centric？}
}
\]

答案：

> Grounding DINO 可以明显改善，至少 Aircraft 已验证。

然后：

\[
\boxed{
\text{Level B：Entity-centric Visual Feature 是否存在稳定内部结构？}
}
\]

答案：

> 存在，Visual Silhouette 显著提高。

最后：

\[
\boxed{
\text{Level C：这些 Visual Structure 是否对应 Text Fine Semantics？}
}
\]

当前答案：

> **尚未证明。独立 KMeans correspondence 很弱，prototype matrix 缺乏 selectivity。**

因此当前论文真正值得继续研究的问题已经变成：

\[
\boxed{
\textbf{如何让视觉实体特征按照文本定义的细粒度语义轴组织，而不是按照自身最显著的视觉变化轴组织？}
}
\]

这比继续提高 Region 定位精度更接近当前核心创新点。

---

# 15. 今日新增/使用的实验脚本

今天围绕该路线新增了以下脚本：

```text
test/
├── c04b_clip_heatmap_sam_refinement.py
├── c04c_clip_points_sam_refinement.py
├── test_grounding_dino_clip_view.py
├── test_grounding_dino_sam.py
├── prototype_feasibility_gdino_teacher.py
└── gdino_object_purification_diagnostic.py

models/
├── sam/
│   └── sam_model.py          # 增加 point prompt 支持
└── grounding_dino/
    ├── __init__.py
    └── grounding_dino_model.py
```

Grounding DINO：

```text
IDEA-Research/grounding-dino-tiny
```

Hugging Face cache：

```text
E:\cunsom_cache\huggingface
```

SAM2：

```text
E:\models\sam2\checkpoints\sam2.1_hiera_base_plus.pt
```

CLIP baseline：

```text
E:\paper3\outputs\clip_rsicd_10ep\best.pth
epoch = 9
```

---

# 16. 明日/下一阶段优先级

推荐严格按照以下顺序推进：

```text
1. Visual → Text Fine Cluster grouped linear probe
        ↓
2. 判断 fine semantics 是否已经存在于 visual feature
        ↓
3A. 若可解码：
    Text-conditioned visual/prototype organization
        ↓
3B. 若不可解码：
    Grounding-to-CLIP local distillation / attribute-aware adaptation
        ↓
4. 重新检查 prototype similarity matrix
        ↓
5. 只有 matrix 出现真实 selectivity 后
    再恢复 GMM / Prototype OT
```

当前不建议增加更多复杂模块。

---

# 17. 今日最终结论

今天最重要的不是找到一个“最终模块”，而是把原问题进一步定位清楚了。

\[
\boxed{
\text{旧 Rectangle 确实导致明显视觉污染}
}
\]

\[
\boxed{
\text{Grounding DINO 能显著改善 object-like Entity 的视觉 proposal}
}
\]

\[
\boxed{
\text{但更好的 Grounding 并不会自动产生更好的 Fine Cross-modal Alignment}
}
\]

因此后续研究重点应该从：

> **“怎样把 Entity 找得更准？”**

逐步转向：

> **“怎样让视觉 Entity representation 按照文本 fine semantics 组织？”**

当前最关键的下一步实验是：

\[
\boxed{
\textbf{Grouped Linear Probe：Visual Feature → Text-defined Fine Cluster}
}
\]

它将决定后续到底应该走：

\[
\text{Text-conditioned Prototype Organization}
\]

还是：

\[
\text{Grounding-to-CLIP Representation Distillation}
\]

这也是今天整个测试链条最终收敛出的下一阶段研究分叉点。
