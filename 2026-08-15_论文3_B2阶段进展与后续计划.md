# 论文3：当前代码结构状态与下一步计划

> 日期：2026-08-15  
> 当前阶段：**B2 — Frozen RSICD CLIP + Residual Adapter + Entity Grounding**  
> 当前状态：**正式 10 Epoch 训练进行中**

---

## 1. 当前项目整体进度

目前方法主线如下：

```text
A. CLIP Global Baseline
        ✅ 已完成
        │
        ▼
LLM Structured Semantics / EAR
Entity + Attribute + Relation
        ✅ 已完成
        │
        ▼
Entity Span Index
caption → token span
        ✅ 已完成
        │
        ▼
CLIP Token / Patch Feature Extraction
        ✅ 已完成
        │
        ▼
Entity-Patch Grounding
        ✅ 已完成
        │
        ├──────────────► B1: Direct Entity Loss
        │                  ✅ 已实现
        │
        ▼
B2: Frozen RSICD CLIP
    + Visual/Text Residual Adapter
    + Global Loss
    + Entity Grounding Loss
        🔄 当前正在正式训练
        │
        ▼
C. Hierarchical Prototype Learning
        ⏳ 下一大阶段
        │
        ▼
GMM Fine Prototype Refinement
        ⏳
        │
        ▼
Prototype-level OT
        ⏳
        │
        ▼
Fine-grained Token/Patch Alignment
        ⏳
```

当前可以概括为：

\[
\boxed{\text{Global CLIP 基础已经完成，现在正在验证 Local Entity 分支是否真正有效}}
\]

Prototype、GMM、OT 尚未正式进入主训练代码。

---

# 2. 当前代码目录结构

当前核心代码大致为：

```text
E:\paper3
│
├── train.py
│
├── configs/
│   └── baseline/
│       └── rsicd_entity_grounding.yaml
│
├── models/
│   ├── backbone.py
│   ├── adapter.py
│   ├── clip_model.py
│   └── __init__.py
│
├── datasets/
│   ├── re_dataset.py
│   └── __init__.py
│
├── losses/
│   └── CLIPLoss
│
├── engine/
│   ├── trainer.py
│   └── scheduler...
│
├── evaluation/
│   ├── retrieval.py
│   └── metrics.py
│
├── tools/
│   ├── structured_semantics/
│   │   └── LLM EAR 流水线
│   │
│   └── entity_prototype/
│       ├── Entity span index 构建
│       └── check_adapter_equivalence.py
│
├── data/
│   └── rsicd/
│       └── entity_prototype/
│           └── rsicd_entity_span_index.pt
│
└── outputs/
    ├── clip_rsicd_10ep/
    │   └── best.pth
    │
    └── clip_rsicd_adapter_entity_10ep/
```

各模块职责已经基本分离：

```text
backbone.py
    ↓
负责 CLIP 特征提取

adapter.py
    ↓
只负责学习特征修正量 Δx

clip_model.py
    ↓
负责 Global / Local 特征组装、
Residual Adapter、Entity Grounding

trainer.py
    ↓
负责 Loss、Backward、Optimizer

retrieval.py
    ↓
负责正式验证 / 测试特征提取和检索

train.py
    ↓
负责整个实验生命周期
```

---

# 3. `models/backbone.py`

## 3.1 当前职责

`backbone.py` 只负责 CLIP 特征提取，不负责：

- Adapter；
- Entity loss；
- Prototype；
- OT；
- Retrieval metrics。

当前能够输出四类表示。

### 图像全局特征

\[
z_v\in\mathbb R^{B\times512}
\]

### 图像 Patch 特征

ViT-B/32 在 224×224 输入下：

\[
P\in\mathbb R^{B\times49\times512}
\]

即 7×7 Patch。

### 文本全局特征

\[
z_t\in\mathbb R^{B\times512}
\]

### 文本 Token 特征

\[
T\in\mathbb R^{B\times77\times512}
\]

---

## 3.2 每个模态只执行一次 Transformer

当前训练中：

```text
Image Transformer
   ↓ 一次
global + patches
```

```text
Text Transformer
   ↓ 一次
global + tokens
```

不会为了 Entity 重新跑 Text Encoder，也不会为了 Patch 重新跑 Vision Encoder。

这是当前代码显存和计算量显著下降的重要原因。

---

## 3.3 支持 `normalize=False`

当前四个接口均支持：

```python
encode_image(..., normalize=False)
encode_image_with_patches(..., normalize=False)
encode_text(..., normalize=False)
encode_text_with_tokens(..., normalize=False)
```

因此当前模型可以执行：

```text
CLIP raw joint-space feature
        ↓
Residual Adapter
        ↓
统一 L2 Normalize
```

而不是：

```text
CLIP Normalize
→ Adapter
→ 再 Normalize
```

---

# 4. `models/adapter.py`

## 4.1 Adapter 最终定义

当前 Adapter 已固定为：

\[
\boxed{\Delta x=A(x)}
\]

Adapter 本身：

- 不做 `x + Δx`；
- 不做 `F.normalize()`；
- 只学习 residual correction。

Visual Adapter：

\[
512\rightarrow128\rightarrow512
\]

Text Adapter：

\[
512\rightarrow64\rightarrow512
\]

基本形式：

```python
Δx = up(GELU(down(x)))
```

---

## 4.2 Zero Initialization

输出层初始化为：

```python
nn.init.zeros_(self.up.weight)
nn.init.zeros_(self.up.bias)
```

因此训练开始时：

\[
A(x)=0
\]

于是：

\[
x+A(x)=x
\]

保证加入 Adapter 后，模型从已有 RSICD baseline 的特征空间出发。

---

## 4.3 Zero-init 等价性已经验证

已有 RSICD baseline：

```text
E:/paper3/outputs/clip_rsicd_10ep/best.pth
```

验证结果：

```text
i2t_r1    : current=13.326551 | saved=13.326551
i2t_r5    : current=24.923703 | saved=24.923703
i2t_r10   : current=36.927772 | saved=36.927772

t2i_r1    : current=14.394710 | saved=14.394710
t2i_r5    : current=37.212614 | saved=37.212614
t2i_r10   : current=53.397762 | saved=53.397762

mR        : current=30.030519 | saved=30.030519

Max metric diff = 0
PASS
```

说明：

\[
\boxed{\text{Zero-init Adapter 不破坏原 RSICD baseline}}
\]

---

# 5. `models/clip_model.py`

`CLIPRetrieval` 当前负责把 Global 和 Local 两条路径组织到统一的 adapted space。

---

## 5.1 图像分支

先从 Backbone 得到：

```text
Global Image Feature
Patch Features
```

然后同一个 Visual Adapter 处理两种视觉特征：

\[
\tilde z_v=
Norm(z_v+A_v(z_v))
\]

\[
\tilde p_j=
Norm(p_j+A_v(p_j))
\]

因此 Global Image 和 Local Patch 位于同一个视觉适配空间。

---

## 5.2 文本分支

Text Transformer 一次得到：

```text
Global Text Feature
Token Features
```

再根据离线 Entity span 从 Token Feature 中做 mean pooling：

\[
e_i=
\frac{1}{|S_i|}
\sum_{k\in S_i} t_k
\]

然后同一个 Text Adapter 处理：

\[
\tilde z_t=
Norm(z_t+A_t(z_t))
\]

\[
\tilde e_i=
Norm(e_i+A_t(e_i))
\]

因此 Global Text 和 Local Entity 同样位于统一适配空间。

---

## 5.3 当前统一特征结构

```text
                 Frozen CLIP
                /           \
           Vision             Text
          /      \           /    \
       Global    Patch     Global  Entity
          │        │          │       │
          └─ Visual Adapter ──┘       │
                            └─ Text Adapter
                                  ↓
                           Adapted Joint Space
```

更加准确地说：

```text
Image Global ─┐
              ├─ Visual Adapter
Patch ────────┘

Text Global ──┐
              ├─ Text Adapter
Entity ───────┘
```

这为后续 Prototype / OT 提供了统一接口。

---

# 6. Entity Span 与 Entity Feature

## 6.1 LLM EAR 已经离线完成

LLM Structured Semantics 当前用于：

\[
\boxed{\text{Entity}+\text{Attribute}+\text{Relation}}
\]

当前 B2 只首先使用 Entity。

LLM 不参与训练时 forward。

---

## 6.2 Compact Entity Span Index

已经把完整 EAR 结果压缩成：

```text
pair_to_semantic
semantic_offsets
span_start
span_end
```

统计：

```text
Total EAR entities : 32667
Valid unique spans : 32182
Valid coverage     : 98.5153%
```

运行时不需要：

- Qwen API；
- 完整 EAR strings；
- 每个 batch JSON 解析；
- 单独 Entity Text Encoder。

---

## 6.3 Dataset 输出

单个训练样本主要返回：

```text
image
caption
image_id
entity_spans
```

Batch collate 后：

```text
images
captions
image_ids
entity_spans
entity_sample_ids
entity_counts
```

---

# 7. Entity-Patch Grounding

对于某个 Entity：

\[
\tilde e_i
\]

只与其对应图像中的 49 个 patch 匹配。

不跨 batch 做 Grounding。

---

## 7.1 Entity-Patch Similarity

\[
s_{ij}
=
\cos(\tilde e_i,\tilde p_j)
\]

因为两侧已经 L2 Normalize，所以实现上直接点积即可。

---

## 7.2 Grounding Attention

当前：

\[
\tau_g=0.02
\]

注意力：

\[
a_{ij}
=
softmax\left(
\frac{s_{ij}}{\tau_g}
\right)
\]

---

## 7.3 Visual Entity Feature

最终：

\[
v_i=
Norm\left(
\sum_j a_{ij}\tilde p_j
\right)
\]

例如：

```text
caption:
"a plane is parked near a runway"

Entity:
plane
runway
```

流程为：

```text
plane
  ↓
与本图 49 个 Patch 比较
  ↓
Attention
  ↓
Visual Plane Feature
```

```text
runway
  ↓
与本图 49 个 Patch 比较
  ↓
Attention
  ↓
Visual Runway Feature
```

---

# 8. 当前 Loss

目前 B2 暂时只使用：

\[
\boxed{
L=
L_{\text{global}}
+
\lambda_e L_{\text{entity}}
}
\]

当前：

\[
\lambda_e=0.1
\]

---

## 8.1 Global Loss

保持已有 CLIP retrieval loss 不变：

\[
L_{\text{global}}
=
L_{\text{current CLIP}}
\]

当前实验没有重新更换 Global Loss。

---

## 8.2 Entity Grounding Loss

单个 Entity：

\[
l_i=
1-\cos(\tilde e_i,v_i)
\]

为了避免 Entity 数量多的 caption 权重天然更大，当前使用 caption-balanced 方式。

对 caption \(b\)：

\[
L_b=
\frac{1}{|E_b|}
\sum_{i\in E_b}
\left(
1-\cos(\tilde e_i,v_i)
\right)
\]

再对有有效 Entity 的 caption 求平均：

\[
L_{\text{entity}}
=
\frac{1}{|V|}
\sum_{b\in V}L_b
\]

---

# 9. 当前到底训练哪些参数

当前正式 B2：

```yaml
freeze_backbone: true
```

因此：

\[
\boxed{\text{CLIP Backbone 完全冻结}}
\]

包括：

- Vision Transformer；
- Text Transformer；
- Projection；
- CLIP logit scale。

当前训练参数只有：

```text
Visual Adapter
+
Text Adapter
```

参数量：

```text
Trainable parameters: 197,824
```

---

## 9.1 已通过梯度检查

1-step Smoke Test：

```text
Gradient check:
Backbone frozen | Adapter active
```

证明：

```text
Frozen Backbone
→ 无梯度

Adapter
→ 正常收到梯度
```

---

# 10. 当前 Baseline 的来源

当前不是从原始 OpenAI CLIP 直接 Freeze。

真实流程：

```text
OpenAI CLIP
    ↓
RSICD Fine-tuning
    ↓
clip_rsicd_10ep/best.pth
    ↓
Val mR = 30.030519
    ↓
加载 checkpoint
    ↓
Freeze Backbone
    ↓
Zero-init Adapter
    ↓
Entity Grounding Training
```

因此当前研究问题更准确地表述为：

\[
\boxed{
\text{在已经具备 RSICD Global Retrieval 能力的 CLIP 上，}
\text{能否通过 Entity-level Adapter 学到额外的局部判别能力？}
}
\]

---

# 11. `engine/trainer.py`

当前 Trainer 已经基本进入正式状态。

流程：

```text
Forward
   ↓
Global Loss
   ↓
Entity Loss
   ↓
L_total
   ↓
Backward
   ↓
Adapter Optimizer
```

---

## 11.1 Frozen Backbone 保持 Eval

训练时首先：

```python
model.train()
```

但冻结 Backbone 后额外：

```python
model.backbone.eval()
```

避免冻结 Backbone 被切回 train mode。

---

## 11.2 首步梯度安全检查

首个 backward 后自动检查：

```text
Backbone 是否意外收到梯度
Adapter 是否收到有效梯度
```

当前已经通过。

---

# 12. 1-Step Smoke Test 结果

当前 B2 已完成 1-step smoke：

```text
Trainable parameters: 197,824

batch size          : 64
valid Entity spans  : 154
valid captions      : 64/64

global loss         : 0.081756
entity loss         : 0.771983
entity loss weight  : 0.1000
weighted entity loss: 0.077198
total loss          : 0.158954

mean Entity cosine  : 0.228261

Gradient check:
Backbone frozen | Adapter active
```

总损失满足：

\[
0.081756
+
0.1\times0.771983
=
0.158954
\]

没有出现 NaN / Inf。

---

## 12.1 显存

```text
CUDA allocated : 0.622 GB
CUDA reserved  : 0.977 GB
CUDA peak      : 0.768 GB
```

冻结 Backbone 后显存明显下降。

---

# 13. `train.py`

当前训练初始化顺序：

```text
读取 YAML
   ↓
构建 CLIP + Adapter
   ↓
加载 RSICD baseline best.pth
   ↓
Adapter 保持 zero-init
   ↓
移动至 GPU
   ↓
Dataset / DataLoader
   ↓
Optimizer / Scheduler
   ↓
Train
   ↓
Validation
   ↓
best.pth / last.pth
```

---

## 13.1 安全限制

如果：

```yaml
freeze_backbone: true
```

但没有：

```yaml
init_checkpoint:
```

会直接报错。

目的：

> 防止意外把原始 OpenAI CLIP 当成当前 RSICD baseline 直接冻结。

---

# 14. `evaluation/retrieval.py`

这是近期发现并修正的一个关键问题。

旧版验证直接：

```python
model.backbone.encode_image(...)
model.backbone.encode_text(...)
```

因此：

```text
训练：
CLIP → Adapter

验证：
CLIP → 直接 Retrieval
```

Adapter 完全被绕开。

现在已经修正为：

```text
Image:
CLIP raw global
→ Visual Adapter
→ Residual
→ Normalize
→ Retrieval

Text:
CLIP raw global
→ Text Adapter
→ Residual
→ Normalize
→ Retrieval
```

因此正式 Validation / Test 会真正评估训练后的 Adapter。

---

# 15. 当前正式实验

当前正在运行：

```text
clip_rsicd_adapter_entity_10ep
```

核心配置：

```yaml
model:
  init_checkpoint: E:/paper3/outputs/clip_rsicd_10ep/best.pth
  freeze_backbone: true
  visual_adapter_dim: 128
  text_adapter_dim: 64
  grounding_temperature: 0.02

training:
  epochs: 10
  batch_size: 64
  eval_batch_size: 128
  text_batch_size: 256
  entity_loss_weight: 0.1

optimizer:
  lr: 1.0e-4
  weight_decay: 0.01

scheduler:
  warmup_ratio: 0.05
```

Baseline 参考：

\[
\boxed{Val\ mR=30.030519}
\]

---

# 16. 当前实验真正要回答的问题

当前 B2 不是单纯为了刷一个更高的数字。

它要回答：

\[
\boxed{
\text{Entity-level Local Supervision}
\text{能否通过轻量 Adapter}
\text{改善已有 RSICD CLIP 的检索空间？}
}
\]

因此本轮训练重点观察：

```text
Global Loss
Entity Loss
Val mR
```

---

# 17. B2 训练结束后的结果判断

## 情况 A：最理想

如果：

\[
L_{\text{entity}}\downarrow
\]

同时：

\[
mR>30.03
\]

说明：

```text
Entity Grounding 有效
+
Adapter 成功将 Local Knowledge
传递到 Global Retrieval Space
```

这时可以直接进入 Prototype 阶段。

---

## 情况 B：Entity 学会了，但 mR 不动

如果：

\[
L_{\text{entity}}\downarrow
\]

但：

\[
mR\approx30.03
\]

说明：

> Local Entity 表示确实在优化，但实例级 Entity 信息尚未有效转化为跨样本检索判别能力。

这种情况下，Prototype 很可能正是需要的桥梁：

```text
Instance Entity
      ↓
Cross-sample Semantic Structure
      ↓
Prototype
      ↓
Global Discrimination
```

---

## 情况 C：Entity Loss 下降但 mR 下降

如果：

\[
L_{\text{entity}}\downarrow
\]

同时：

\[
mR\downarrow
\]

说明：

> Local Entity Supervision 和 Global Retrieval Objective 存在明显张力。

此时才考虑：

```text
Residual Scale α
Entity Loss Weight λe
CLIP tiny LR
Partial Unfreeze
```

当前不提前增加这些变量。

---

# 18. B2 结束后第一步：完整训练曲线分析

训练完成后不只看：

```text
best mR
```

而是整理：

```text
Epoch
Train Total Loss
Global Loss
Entity Loss
I2T Loss
T2I Loss

Val I2T R@1
Val I2T R@5
Val I2T R@10

Val T2I R@1
Val T2I R@5
Val T2I R@10

Val mR
```

重点分析：

\[
L_e
\]

与：

\[
mR
\]

之间的关系。

---

# 19. B2 结束后的第二步：Test Set

选取最佳 Val checkpoint：

```text
best.pth
```

在 RSICD Test 上正式测试：

```text
I2T R@1 / R@5 / R@10
T2I R@1 / R@5 / R@10
mR
```

与原 CLIP baseline 做严格对比。

---

# 20. B2 结束后的第三步：Entity Grounding 可视化

当前 Entity loss 下降只能证明：

\[
e_i
\]

与：

\[
v_i
\]

越来越接近。

但还不能直接证明：

> airplane 真正在看 airplane patch。

因此下一步需要开发：

```text
tools/entity_prototype/visualize_entity_grounding.py
```

---

## 20.1 建议样例

选择 5～10 张具有明确实体的 RSICD 图像，例如：

```text
airplane / runway / building
ship / port / sea
bridge / river / trees
stadium / road / buildings
```

避免：

```text
area
scene
land
```

等语义过于模糊的 Entity。

---

## 20.2 可视化内容

每个 Entity：

```text
7×7 Grounding Heatmap
Top-3 Patch
Top-5 Patch
```

最好比较：

```text
Baseline / Zero-init
        VS
B2 best checkpoint
```

从而判断：

\[
\boxed{\text{Entity Grounding 是否真正获得位置感知能力}}
\]

这部分后续也可以作为论文 Visualization。

---

# 21. 下一大阶段：Prototype Learning

B2 稳定后进入：

\[
\boxed{\text{C：Hierarchical Prototype Learning}}
\]

Entity Grounding 当前仍然属于：

\[
\text{Instance-level Alignment}
\]

例如：

```text
当前 caption 的 airplane
↕
当前 image 的 airplane-like patches
```

但还没有解决跨样本结构：

```text
airplane
aircraft
fighter jet
commercial plane
parked aircraft
...
```

因此下一步需要：

\[
\boxed{
\text{Instance Entity}
\rightarrow
\text{Semantic Prototype}
}
\]

---

# 22. Prototype 阶段建议拆分

不要一次性把：

```text
Prototype
+ GMM
+ OT
+ Fine Alignment
```

全部加入。

建议逐阶段验证。

---

## 22.1 C1：Basic Prototype Bank

先做：

```text
Adapter
+
Entity Grounding
+
Basic Prototype Bank
```

验证：

```text
Prototype 能否稳定更新
Assignment 是否合理
Prototype Loss 是否稳定
Retrieval 是否被破坏
```

---

## 22.2 C2：Hierarchical / GMM Fine Prototype

再解决：

> 一个粗语义类别内部包含多个细粒度模式。

例如：

```text
aircraft
 ├─ fighter aircraft
 ├─ commercial aircraft
 └─ parked aircraft
```

由此：

```text
Coarse Prototype
      ↓
GMM
      ↓
Fine Prototypes
```

---

# 23. Prototype-level OT

有了 Text / Visual Fine Prototype 后，再进入：

\[
\boxed{\text{Prototype-level OT}}
\]

定义代价：

\[
C_{ij}
=
1-\cos(\mu_i^t,\mu_j^v)
\]

得到：

\[
\Pi^*
\]

目标不是强制：

```text
Text Prototype
↔
Visual Prototype
```

一一对应，而是学习：

\[
\boxed{\text{Soft Many-to-Many Cross-modal Alignment}}
\]

这正好处理遥感图文中的细类非一一对应问题。

---

# 24. 最后的 Fine-grained Alignment

进一步把 Prototype-level OT：

\[
\Pi^*
\]

投影回 Token / Patch 层。

最终形成：

```text
Global CLIP Alignment
        ↓
Entity Grounding
        ↓
Prototype Structure
        ↓
Prototype OT
        ↓
Token / Patch Fine-grained Alignment
```

这才是完整的大一统模型。

---

# 25. 后续开发路线

从现在开始建议固定为：

```text
当前：
B2 Frozen CLIP
+ Residual Adapter
+ Entity Grounding
        ↓
10 Epoch 正在训练
        ↓
完整曲线分析
        ↓
Best Checkpoint Test
        ↓
Entity Grounding Visualization
        ↓
判断 B2 是否成立
        ↓
C1 Basic Prototype Bank
        ↓
Prototype Assignment / Update Validation
        ↓
C2 Hierarchical + GMM Fine Prototype
        ↓
D Prototype-level OT
        ↓
Token/Patch Fine-grained Alignment
        ↓
完整消融实验
```

---

# 26. 当前阶段应冻结的代码

B2 正式训练期间，除非发现明确 Bug，以下文件建议暂时不再改：

```text
models/backbone.py
models/adapter.py
models/clip_model.py
engine/trainer.py
train.py
evaluation/retrieval.py
```

原因是：

> 当前最重要的是获得一个干净、可解释的 B2 实验结果，而不是继续改变结构。

---

# 27. 当前阶段的核心研究问题

最终可将当前 B2 概括为：

\[
\boxed{
\text{Frozen RSICD CLIP}
+
\text{Asymmetric Residual Feature Adapter}
+
\text{Entity-Patch Grounding}
}
\]

训练目标：

\[
\boxed{
L=
L_{\text{global}}
+
\lambda_eL_{\text{entity}}
}
\]

当前实验要验证：

\[
\boxed{
\text{Entity-level Local Knowledge}
\overset{\text{Adapter}}{\longrightarrow}
\text{Global Retrieval Improvement?}
}
\]

这个问题得到明确答案后，才进入：

\[
\boxed{
\text{Entity}
\rightarrow
\text{Prototype}
\rightarrow
\text{Fine Prototype}
\rightarrow
\text{OT}
}
\]

---

# 28. 当前结论

截至目前，项目已经完成：

```text
数据准备                      ✅
LLM EAR                      ✅
Entity Span                  ✅
CLIP Global/Token/Patch      ✅
Entity Grounding             ✅
Compact Training Pipeline    ✅
Residual Adapter             ✅
Frozen Backbone Training     ✅
Gradient Validation          ✅
Zero-init Equivalence        ✅
Formal Retrieval Path        ✅
B2 1-step Smoke Test         ✅

B2 10-Epoch Training         🔄
Prototype                    ⏳
GMM                          ⏳
OT                           ⏳
Fine Alignment               ⏳
```

当前不再属于基础骨架开发阶段，而是：

\[
\boxed{\textbf{B2 方法验证阶段}}
\]

短期最重要目标：

\[
\boxed{
\text{先用当前 10-Epoch 实验判断 Entity + Adapter 是否真正提高检索能力}
}
\]

如果 B2 成立，下一阶段直接进入 **Prototype Learning**。
