# 论文3：后续研究思路与参考论文整理
**日期：2026-08-17**

---

## 1. 当前研究状态：问题已经收敛，而不是“无路可走”

经过近期一系列实验，论文3的核心问题已经从最初较宽泛的：

\[
\text{如何提升遥感图文检索中的细粒度对齐？}
\]

逐步收敛为：

\[
\boxed{
\textbf{如何让视觉 Entity representation 按照文本定义的细粒度语义轴组织？}
}
\]

目前已经较明确地排除了若干不成立或收益有限的假设：

```text
Raw CLIP Patch
→ Entity discrimination 很弱

Multi-scale Rectangle
→ 能提供局部信息
→ 但受尺度、背景、场景上下文污染

CLIP Top Region → SAM
→ Semantic purity 不等于 object completeness

CLIP Heatmap / Point Prompt
→ Region score/center 不能直接等价为真实 spatial grounding

Grounding DINO
→ 能显著改善 object-like entity 的 proposal quality
→ 但更好的 Grounding 不会自动产生更好的 fine cross-modal alignment
```

因此现在已经不是继续“找更漂亮的框”的阶段。

真正需要研究的是：

\[
\boxed{
\text{Visual feature 中是否已经存在 Text 所关心的 fine semantics？}
}
\]

以及：

\[
\boxed{
\text{如果存在，如何将视觉表示沿 Text semantic axis 重新组织？}
}
\]

---

# 2. 重新定义论文主线

原始的大一统路线大致是：

```text
CLIP Global
+
LLM Entity Grounding
+
Hierarchical Prototype
+
GMM
+
Prototype OT
+
Token/Patch Alignment
```

目前不建议继续强行推进全部模块。

更合理的主线应收缩为：

\[
\boxed{
\text{Global CLIP Anchor}
\rightarrow
\text{Structured Text Semantics}
\rightarrow
\textbf{Text-Conditioned Visual Organization}
\rightarrow
\text{Intra-category Fine Matching}
}
\]

其中：

- Global CLIP：保留原始检索能力；
- Structured Text Semantics：LLM EAR 提取 Entity / Attribute / Relation；
- Text-Conditioned Visual Organization：让视觉 representation 根据文本关注的语义重新组织；
- Fine Matching：重点解决 same-category instance discrimination。

Prototype、GMM、OT 暂时全部降级为后续可选模块。

---

# 3. 当前真正需要回答的核心问题

目前实验已经表明：

\[
\text{Visual Structure}
\neq
\text{Text Fine Semantic Structure}
\]

视觉侧更容易按照：

```text
orientation
object size
crop completeness
background
scene scale
proposal granularity
instance count
```

形成主变化轴。

文本侧则更关注：

```text
large / small
parked
curved
circular
red
multiple
beside
surrounded by
```

因此：

\[
\boxed{
\text{Visual KMeans cluster}
\neq
\text{Text KMeans cluster}
}
\]

并不能直接说明：

> 视觉 feature 中没有 fine semantics。

更可能是：

> 视觉中存在这些信息，但其 dominant axis 与文本语义轴不同。

这正是下一阶段需要验证的。

---

# 4. 下一步最重要的决策实验：Grouped Linear Probe

## 4.1 研究问题

不要再问：

> Text 和 Visual 独立 KMeans 后会不会自动聚成一样？

而应该问：

\[
\boxed{
\text{Text 定义出的 fine semantic cluster}
\text{能否从 Visual feature 中被解码？}
}
\]

---

## 4.2 Text Side

文本仍然使用：

\[
\text{Entity + color/size/shape/state}
\]

得到：

\[
t_i
\]

然后进行 Text KMeans：

\[
t_i\rightarrow y_i^{text}
\]

其中：

\[
y_i^{text}\in\{1,\dots,K\}
\]

表示文本侧定义出的 fine semantic cluster。

---

## 4.3 Visual Side

分别测试：

```text
Raw GDINO Mean
Filtered Top1
Filtered Mean
```

得到：

\[
v_i
\]

这一次不对视觉做 KMeans。

直接训练一个最简单的 frozen-feature linear probe：

\[
\hat y_i=Wv_i+b
\]

预测：

\[
y_i^{text}
\]

只训练这一层。

---

## 4.4 必须做 Group Split

当前文本侧存在大量重复或近重复 phrase，例如：

```text
planes
parked planes
parked plane
```

因此不能进行普通随机 sample split。

必须使用：

\[
\boxed{\text{Group split by fine phrase}}
\]

例如 `"parked planes"` 出现 10 次：

```text
要么全部进入 train
要么全部进入 test
```

不能拆开。

否则 probe accuracy 可能因为 lexical/template leakage 虚高。

---

# 5. Linear Probe 的两个分叉

## 情况 A：Probe 明显高于 baseline

例如：

```text
K=4

Majority / Random baseline ≈ 25%

Raw Visual        28%
Filtered Top1     31%
Filtered Mean     45%
```

则说明：

\[
\boxed{
\text{Fine semantic information 已经存在于 CLIP visual feature 中}
}
\]

只是：

\[
\boxed{
\text{Visual 自己的 dominant axis 不是 Text semantic axis}
}
\]

这时候后续主方向应该是：

\[
\boxed{
\textbf{Text-Conditioned Visual Organization}
}
\]

而不是重新训练整个视觉 backbone。

---

## 情况 B：Probe 接近 baseline

例如：

```text
Raw             ≈ 25%
Filtered Top1   ≈ 25%
Filtered Mean   ≈ 27%
```

则说明：

\[
\boxed{
\text{当前 CLIP visual feature 中缺乏所需 fine semantics}
}
\]

此时才进入：

\[
\boxed{
\textbf{Grounding-to-CLIP Distillation / Local Adaptation}
}
\]

重点提升 CLIP 自身 local representation。

---

# 6. 第一主路线：Text-Conditioned Visual Organization

如果 linear probe PASS，这是目前最值得押的方向。

假设视觉候选为：

\[
V=\{v_1,v_2,\dots,v_M\}
\]

文本 Entity+Attribute 为：

\[
q
\]

不要再独立聚类视觉。

直接利用文本决定如何读取视觉信息：

\[
a_m=
\operatorname{softmax}
\left(
\frac{
(W_q q)^\top(W_vv_m)
}{\tau}
\right)
\]

然后：

\[
\boxed{
v_q=\sum_m a_mv_m
}
\]

这样，同一张图对于不同文本 query 会得到不同视觉表示。

例如：

```text
"airplane"
→ representation A

"large airplane"
→ representation B

"parked airplane"
→ representation C
```

因此视觉 representation 不再是固定：

\[
v_{\text{aircraft}}
\]

而是：

\[
\boxed{
v(I,q)
}
\]

即：

> 同一张图的视觉表示随着文本关注的细粒度语义变化。

---

# 7. 为什么 FLAIR 是当前最重要的参考论文

## FLAIR

**FLAIR: VLM with Fine-grained Language-informed Image Representations**

核心思想：

> 一个固定 image embedding 很难表达所有细粒度信息，因此使用文本条件的 attention pooling，在 local visual tokens 上生成 text-specific image representation。

可以粗略写成：

```text
Text / Sub-caption
      ↓
Condition Local Visual Tokens
      ↓
Text-specific Image Representation
```

这和当前问题高度一致。

### 我们可以借鉴

- text-conditioned attention pooling；
- text-specific image representation；
- local visual token 的条件聚合；
- 不要求视觉侧独立形成与文本一致的 cluster。

### 我们不能照搬

FLAIR 更偏：

```text
sub-caption-conditioned representation
```

而我们的潜在差异可以是：

\[
\boxed{
\text{Structured Entity–Attribute–Relation conditioned representation}
}
\]

并且任务目标是：

\[
\boxed{
\text{RSITR intra-category instance discrimination}
}
\]

而不是一般视觉语言预训练。

---

# 8. RegionCLIP：解释为什么 Global CLIP 不能直接当 Region CLIP

## RegionCLIP

**RegionCLIP: Region-based Language-Image Pretraining**

一个非常重要的思想是：

> CLIP 是 whole-image–text 训练的，直接把它应用到 local regions 会存在 region-language domain shift。

这和当前实验非常吻合：

```text
Raw Patch 不好
Rectangle Crop 有 context contamination
GDINO Crop 更 object-centric
但 Local CLIP / Text 仍有明显 modality gap
```

### 对我们的启发

不要默认：

\[
\boxed{
\text{Global CLIP representation}
=
\text{Good Region representation}
}
\]

如果后续 probe FAIL，那么 region-level representation adaptation 是合理方向。

---

# 9. 第二主路线：Grounding-to-CLIP Distillation

如果 linear probe FAIL，则说明：

\[
\text{当前 CLIP local visual feature 本身不够}
\]

这时 Grounding DINO 最合理的角色不是 inference-time module，而是：

\[
\boxed{
\text{Training-time Teacher}
}
\]

训练：

```text
Entity
 ↓
Grounding DINO
 ↓
Pseudo Region / Box
 ↓
Teacher Region Feature
        ↓
   Distillation
        ↓
CLIP Local Student
```

最终测试阶段：

```text
× Grounding DINO
× SAM

Image → CLIP-based Retrieval Model
Text  → CLIP-based Retrieval Model
```

这样才能证明：

\[
\boxed{
\text{External Knowledge}
\rightarrow
\text{Internalized Capability}
}
\]

而不是单纯依赖一个强外部模型。

---

# 10. ViLD：Teacher Distillation 的重要参考

## ViLD

**Open-vocabulary Object Detection via Vision and Language Knowledge Distillation**

ViLD 的主要启发是：

> 利用强 vision-language teacher 的语义知识，蒸馏到 region-level student representation。

对我们可以类比为：

```text
Grounding DINO / CLIP teacher region
        ↓
pseudo local supervision
        ↓
our CLIP local branch
```

如果最终 inference 不再调用 GDINO，那么这条路线在论文逻辑上会比直接外挂 detector 更干净。

---

# 11. CLIPSelf 与 FarSLIP：Local Representation Adaptation

## CLIPSelf

核心问题：

> CLIP 从 image-level representation 转移到 region/dense representation 时存在明显 domain shift。

其思路通过 crop/image self-distillation 改善 dense/local representation。

### 借鉴点

- global → local representation adaptation；
- self-distillation；
- 不依赖额外标注；
- 保持 CLIP 原有语义能力。

---

## FarSLIP

FarSLIP 更直接针对遥感领域。

值得关注：

- RS-CLIP 的 fine-grained spatial awareness 不足；
- 直接 region-text alignment 可能损害 semantic coherence；
- patch-to-patch distillation；
- CLS-based region-category alignment；
- global semantics 与 local awareness 解耦。

### 我们和 FarSLIP 的潜在差异

FarSLIP 更偏：

\[
\text{Improve local representation quality}
\]

而我们真正想做：

\[
\boxed{
\text{Structured textual semantics}
\rightarrow
\text{Organize local visual variation}
}
\]

即：

> 不只是让 local feature 更强，而是让它沿文本关心的 semantic axis 组织。

---

# 12. FILIP：一个值得做的 Local Alignment Baseline

## FILIP

核心：

\[
S(I,T)
\sim
\sum_i\max_j\cos(t_i,v_j)
\]

也就是 token-wise late interaction。

### 可以借鉴

- token-region local matching；
- 不需要大型 cross-attention；
- 适合作为简单 local baseline。

### 但不能直接作为最终方案

我们已经看到：

\[
\boxed{
\max\text{ similarity}
}
\]

很容易选中：

```text
semantic-pure but incomplete region
```

因此我们更需要：

\[
\boxed{
\text{Structured Text-conditioned Aggregation}
}
\]

而不是 independent max matching。

---

# 13. 遥感领域最值得重点跟进的论文

## 13.1 FarSLIP

重点看：

- 遥感 CLIP local adaptation；
- semantic coherence preservation；
- region-level spatial awareness；
- local/global feature 解耦。

---

## 13.2 MPS-CLIP

核心思路：

```text
LLM / keyword
→ SamGeo subimages
→ multi-perspective representation
→ RSITR
```

和我们的距离较近。

必须明确后续差异：

### MPS-CLIP

更偏：

\[
\text{External Region / Perspective Extraction}
\]

### 我们希望做到

\[
\boxed{
\text{Structured Semantics}
\rightarrow
\text{Teach / organize CLIP's own fine representation}
}
\]

最好最终 inference 不依赖外部 grounding。

---

## 13.3 GeoAlignCLIP

重点关注：

- multi-granular semantic alignment；
- fine-grained region concept alignment；
- intra-modal consistency；
- hard negative；
- region-level annotation / benchmark。

这篇和我们的“多粒度 + fine alignment”概念距离较近。

后续必须认真确认：

> 我们的创新是否与其已有设计重合。

---

# 14. Fast-then-Fine：未来 Fine Reranking 的参考

当前 C1-R 已经验证：

```text
Global CLIP
→ Top-N
→ Local rerank
```

总体框架并没有错。

失败原因是：

> 当前 local score 还不够可靠。

未来如果能学习出稳定：

\[
S_{\text{fine}}(I,T)
\]

最终完全可以采用两阶段：

\[
\boxed{
\text{Global Recall}
\rightarrow
\text{Fine Reranking}
}
\]

即：

\[
S_{\text{final}}
=
S_{\text{global}}
+
\lambda S_{\text{fine}}
\]

只作用于 Top-50 / Top-100。

这样：

- 保留 global CLIP retrieval capability；
- fine branch 只负责 hard intra-category discrimination；
- computational cost 可控；
- 不需要重新编码整个 gallery。

---

# 15. River / Road 等 Region-like Entity 的备用方向

当前不是主线，但未来如果发现大量 Entity 不适合 object box，可以考虑：

## OpenSeg

核心思想：

```text
Visual Masks
+
Caption Words
→ region-text alignment
```

对：

```text
river
road
forest
water
grass
```

这种 stuff / irregular region，比矩形 object detector 更自然。

---

## GroupViT

核心启发：

> image-text supervision 本身也可以诱导 visual grouping，形成不规则 semantic regions。

这类方法对：

```text
river / road / vegetation
```

比 Grounding DINO box 更有理论参考意义。

但目前不建议马上展开。

---

# 16. Prototype / GMM / OT 应该什么时候恢复？

当前不要继续：

```text
Prototype
→ GMM
→ OT
```

因为现在 prototype similarity matrix：

- 比较平；
- 存在 prototype hub；
- PairCons 接近随机 baseline；
- Text / Visual independent clustering 轴不一致。

OT 无法从一个本身没有明显 selectivity 的 cost matrix 中创造 correspondence。

正确顺序应该是：

```text
Fine semantics 可解码
        ↓
Text-conditioned visual organization
        ↓
Prototype matrix 出现真正 selectivity
        ↓
再考虑：
Sub-prototype
GMM
OT
```

因此：

\[
\boxed{
\text{Prototype / OT 是后置工具，不应再当主线起点}
}
\]

---

# 17. 未来研究路线图

## Stage 1：验证 Fine Semantics 是否存在

```text
Grouped Linear Probe
Visual Feature
    ↓
Text-defined Fine Cluster
```

### PASS

进入：

\[
\boxed{
\text{Text-Conditioned Visual Organization}
}
\]

参考：

- FLAIR；
- FILIP；
- text-conditioned attention / pooling。

---

### FAIL

进入：

\[
\boxed{
\text{Grounding-to-CLIP Local Distillation}
}
\]

参考：

- RegionCLIP；
- ViLD；
- CLIPSelf；
- FarSLIP。

---

# 18. Stage 2：Fine Representation 用于 Retrieval

不直接替换 global CLIP。

推荐：

```text
Image/Text
   ↓
Global CLIP
   ↓
Top-M Candidates
   ↓
Structured Fine Matching
   ↓
Rerank
```

核心目标：

\[
\boxed{
\text{Same-category Hard Negative Discrimination}
}
\]

而不是重新做一遍 coarse semantic retrieval。

---

# 19. Stage 3：Prototype 重新进入

只有当：

```text
Text-conditioned visual feature
```

已经明显具备：

```text
fine semantic structure
```

并且：

```text
prototype similarity matrix
```

出现明显选择性后，才重新引入：

```text
Hierarchical Prototype
↓
Sub-prototype
↓
GMM
↓
Prototype OT
```

此时 OT 才真正有意义。

---

# 20. 当前建议暂停的方向

建议暂时冻结：

```text
× SAM 主线
× CLIP Heatmap → Box
× CLIP Region Center → Point Prompt
× 继续调 GDINO threshold
× 继续调 box area threshold
× Entity → Raw Patch 强对齐
× Text / Visual 独立 KMeans 后强行对应
× 立即上 GMM
× 立即上 OT
× 无条件 Global + Local Score Fusion
```

原因是：

\[
\boxed{
\text{当前主要矛盾已经不是“找到一个更漂亮的区域”}
}
\]

而是：

\[
\boxed{
\text{Visual representation 是否包含并能否沿 Text fine semantic axis 被读取}
}
\]

---

# 21. 参考论文阅读优先级

如果只优先精读 6 篇：

## 第一优先级

### 1. FLAIR
重点：

- text-conditioned attention pooling；
- text-specific image representation；
- fine-grained language-informed visual representation。

### 2. RegionCLIP
重点：

- region-language domain shift；
- global CLIP → region representation；
- region-text alignment。

### 3. FarSLIP
重点：

- remote sensing local visual adaptation；
- semantic coherence preservation；
- local/global 解耦。

### 4. GeoAlignCLIP
重点：

- multi-granular alignment；
- fine region concept；
- intra-modal consistency；
- 与我们潜在重合点。

### 5. MPS-CLIP
重点：

- keyword / LLM-guided subimage；
- SamGeo；
- multi-perspective RSITR；
- 明确我们不能只做 external region extraction。

### 6. ViLD
重点：

- vision-language teacher → region student distillation；
- 如果 linear probe FAIL，作为主要参考。

---

# 22. 第二梯队论文

后续补充：

```text
FILIP
CLIPSelf
OpenSeg
GroupViT
Fast-then-Fine
```

用途分别是：

```text
FILIP
→ local late interaction baseline

CLIPSelf
→ image-level → dense/local representation adaptation

OpenSeg
→ irregular/stuff region semantic alignment

GroupViT
→ text-supervised visual grouping

Fast-then-Fine
→ coarse recall + fine reranking
```

---

# 23. 当前最看好的最终论文形态

不建议最终变成：

```text
CLIP
+ LLM
+ GDINO
+ SAM
+ Prototype
+ GMM
+ OT
+ ...
```

这种路线容易被质疑：

> 模块堆叠太多，真正贡献不明确。

当前更有希望的是：

\[
\boxed{
\textbf{Structured Text-Conditioned Fine-Grained Retrieval}
}
\]

最终核心：

```text
Clean Global CLIP
+
LLM Structured Semantics
+
Text-Conditioned Local Visual Representation
+
Intra-category Fine Reranking
```

Grounding DINO只保留为：

\[
\boxed{
\text{Diagnostic / Training Teacher}
}
\]

Prototype如果最终重新加入，也应该是：

\[
\boxed{
\text{Text-conditioned Prototype}
}
\]

而不是 Text / Visual 两边先独立盲聚，再用 OT 强行对应。

---

# 24. 当前最核心的一句话

后续研究不要再问：

> **“视觉自己能聚成什么？”**

而应该问：

\[
\boxed{
\textbf{“文本关心的语义能否从视觉中读出来，以及如何把它读出来？”}
}
\]

这是当前实验链条最终共同指向的问题。

下一项信息增益最高的实验是：

\[
\boxed{
\textbf{Grouped Linear Probe：
Visual Feature → Text-defined Fine Cluster}
}
\]

这项实验将决定论文3下一阶段真正进入：

\[
\text{Text-conditioned Visual Organization}
\]

还是：

\[
\text{Grounding-to-CLIP Representation Distillation}
\]

它也是当前整个项目最重要的研究分叉点。
